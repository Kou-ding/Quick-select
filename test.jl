A = [1, 2, 3, 4, 5]

# Get a pointer to the first element
ptr_to_first = pointer(A)

# Get a pointer to the last element
ptr_to_last = pointer(A[end])

# Display the pointers
println(ptr_to_first)
println(ptr_to_last)
